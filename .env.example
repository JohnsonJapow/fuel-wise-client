VITE_GOOGLE_MAPS_API_KEY=
# Only needed in production builds (Render frontend service); leave blank locally.
VITE_API_BASE_URL=
# Optional: override the local dev proxy target (defaults to http://localhost:8080).
VITE_DEV_PROXY_TARGET=
